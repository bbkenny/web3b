# TownPlanPay Frontend — Environment Variables Example
# 
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to git (it's in .gitignore)

# API Configuration
VITE_API_URL=http://localhost:3000

# Environment
VITE_ENVIRONMENT=development

# Optional: Analytics
# VITE_ANALYTICS_ID=

# Optional: Feature Flags
# VITE_ENABLE_ANALYTICS=true
# VITE_ENABLE_BETA_FEATURES=false

# Optional: Blockchain Network
# VITE_ARC_RPC_URL=https://arc-testnet-rpc.example.com
# VITE_CHAIN_ID=123

# Optional: IPFS Gateway (for photos)
# VITE_IPFS_GATEWAY=https://gateway.pinata.cloud

# Optional: Contact Info for Support
# VITE_SUPPORT_EMAIL=support@townplanpay.io
# VITE_DISCORD_URL=https://discord.gg/...

# Notes:
# - VITE_ prefix exposes variables to frontend code
# - Access via: import.meta.env.VITE_API_URL
# - Never commit real secrets to git
# - Use different .env files per environment:
#   - .env (shared, version controlled)
#   - .env.local (local overrides, not committed)
#   - .env.development (dev-specific)
#   - .env.production (prod-specific)
